# OLE 回归样本目录

用于 `DOC/PPT/XLS` 提取回归样本管理。

目录约定：
- `doc/`：`.doc` 样本
- `ppt/`：`.ppt` 样本
- `xls/`：`.xls` 样本

数量目标（M5）：
- 每类样本 `>= 20`

计数校验：
```bash
bash tests/fixtures/ole/check_counts.sh
```

说明：
- 样本文件优先放置可公开分发或脱敏后的测试文件。
- 建议覆盖：中英文混排、空文档、损坏文件、超大文件、边界编码、含公式/图表/嵌入对象等。
