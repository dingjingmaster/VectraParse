[package]
name = "vectraparse-parsers"
version = "0.1.0"
edition.workspace = true
license.workspace = true

[dependencies]
vectraparse-core.workspace = true
vectraparse-mime.workspace = true
zip = { version = "2.4.2", default-features = false, features = ["deflate"] }
